{% extends 'base.html.twig' %}

{% block title %}Precious{% endblock %}

{% block body %}
    <h1>Precious</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preciou.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ preciou.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ preciou.description }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ preciou.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_precious_index') }}">back to list</a>

    <a href="{{ path('app_precious_edit', {'id': preciou.id}) }}">edit</a>

    {{ include('precious/_delete_form.html.twig') }}
{% endblock %}
